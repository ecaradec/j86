FUNCTION sum(a, b) {
    RETURN a + b;
}

FUNCTION fact(i) {
    IF( i == 1 ) { 
        RETURN 1;
    }
    RETURN i * fact(i-1);
}

FUNCTION main() {
    a = 1;

    IF( a == 1 ) {
        printf("IF OK");
    } ELSE {
        printf("IF NOK");
    }

    a = sum(1,1);
    IF( a == 2) {
        printf("CALL OK");
    } ELSE {
        printf("CALL NOK");
    }

    a = 5;
    WHILE( a != 0 ) {
        a = a - 1;
    }
    IF( a == 0 ) {
        printf("WHILE OK");
    } ELSE {
        printf("WHILE NOK");
    }

    a = fact(5);
    IF( a == 120 ) {
        printf("RECURSION OK");
    } ELSE {
        printf("RECURSION NOK");
    }

    a = 1 + 1;
    b = 1 + 1;
    c = 1 + 1;
    d = 1 + 1;
    g = a+b+c+d;
    z = d+d;
    IF(g == 8) {
        printf("SPILL OK");
    } ELSE {
        printf("SPILL NOK");
    }
}